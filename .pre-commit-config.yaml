repos:
  - repo: local
    hooks:
      - id: tests
        name: Run test suite
        entry: make test
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
